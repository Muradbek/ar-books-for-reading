# Исходные экспорты «Шамили» — нужны только локально, для сборки.
# Только в корне: страницы сайта в docs/ должны попадать в репозиторий.
/*.htm
/*.html
/*/*.htm

# Мастер-копии EPUB в корне: на сайт попадают их копии в docs/books/
/*.epub

# Личное и локальное
MD_HISTORY/
.claude/
